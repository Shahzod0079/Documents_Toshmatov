<UserControl x:Class="Documents_Toshmatov.Elements.Item"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="221" 
             Width="461">
    <Border BorderBrush="LightGray" 
            BorderThickness="1" 
            Margin="0,70,0,0" 
            Padding="10" 
            Background="White" 
            Height="146" 
            VerticalAlignment="Top" 
            HorizontalAlignment="Center" 
            Width="440">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Левая часть - изображение -->
            <Image x:Name="img" 
                   Source="/Image/icon-black.png" 
                   Grid.Column="0" 
                   Margin="0,25,0,40" 
                   Width="50" 
                   Height="50"/>

            <!-- Правая часть - информация о документе -->
            <Grid Grid.Column="1" 
                  HorizontalAlignment="Center" 
                  Width="363" 
                  Height="127" 
                  VerticalAlignment="Bottom">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Строка 1: Наименование и Дата -->
                <TextBlock x:Name="lName" 
                           Text="Наименование" 
                           FontWeight="Bold" 
                           FontSize="14" 
                           Grid.Row="0"/>
                <TextBlock x:Name="lDate" 
                           Text="Дата поступления: 00.00.0000" 
                           Grid.Row="0" 
                           HorizontalAlignment="Right"/>

                <!-- Строка 2: Ответственный и Статус -->
                <TextBlock x:Name="lUser" 
                           Text="Ответственный: Иванов И.И." 
                           Grid.Row="1"/>
                <TextBlock x:Name="lStatus" 
                           Text="Статус: Входящий" 
                           Grid.Row="1" 
                           HorizontalAlignment="Right"/>

                <!-- Строка 3: Код документа и Направление -->
                <TextBlock x:Name="lCode" 
                           Text="Код документа: 000-000" 
                           Grid.Row="2"/>
                <TextBlock x:Name="lDirect" 
                           Text="Направление: Строительство" 
                           Grid.Row="2" 
                           HorizontalAlignment="Right"/>

                <!-- Строка 4: Кнопки -->
                <StackPanel Orientation="Horizontal" 
                            Grid.Row="3" 
                            HorizontalAlignment="Right" 
                            Margin="0,10,0,0">
                    <Button x:Name="btnEdit" 
                            Content="Изменить" 
                            Click="EditDocument_Click" 
                            Margin="0,0,10,0" 
                            Width="80"/>
                    <Button x:Name="btnDelete" 
                            Content="Удалить" 
                            Click="DeleteDocument_Click" 
                            Width="80"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>